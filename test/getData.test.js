const getData = require('./getData.test');

const assert = require('assert');
const _ = require('underscore');

describe('getData service testing', async function () {

  it('test getPosts return posts', async function () {
/*
    assert.ok(
      web3Staking.stakingWithSigner,
      "web3Staking ethers connection ready this.stakingWithSigner,"
    );
*/
  });

  it('getUser return user data speified by id', async function () {
  });

  it('getCombinedData return user and post data combined', async function () {
  });

});
